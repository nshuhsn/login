mixin video(video)
    div
            h4
                //- href,class,id 같은곳에는 #{} 문법이 불가능!
                a(href=`/videos/${video.id}`)=video.title
            p=video.description
            small=video.createdAt
            hr